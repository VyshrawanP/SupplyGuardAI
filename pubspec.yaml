name: supply_guard_ai
description: Real-Time Disaster Logistics Command System
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The root Flutter target is a launcher for the main app in `frontend/`.
  supplyguard_ai_frontend:
    path: frontend

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
